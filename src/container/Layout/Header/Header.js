import React from 'react';
import Navigation from "../../../component/Navigation/Navigation";

const Header = (props) => {
    return (
        <header>
            <nav>
                <Navigation />
            </nav>
        </header>
    )
}

export default Header;